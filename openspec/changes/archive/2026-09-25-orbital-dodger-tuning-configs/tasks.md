## 1. Database and repository

- [x] 1.1 Add migration `0039_orbital_dodger_configs` in `src/db.ts`. It creates `game_od_configs` with the case-insensitive unique name index and the partial unique `is_default` index, and seeds `('Default', '{}', is_default=1)`. Add the table to `TABLE_NAMES`. Verify: a fresh test DB has exactly one default row, and the existing db/export tests pass.
- [x] 1.2 Add `IOrbitalConfigRepository` and the `OrbitalConfig` type to `src/repositories/interfaces.ts`. Implement `SqliteOrbitalConfigRepository` (`list`, `get`, `create`, `update`, `delete`) in `src/repositories/sqlite/orbitalConfigs.ts`. It returns typed results for not-found, name-taken and is-default, instead of throwing. Verify: `orbitalConfigs.test.ts` covers ordering (the Default first), a case-insensitive duplicate name, refusal to rename or delete the Default, and a tuning round-trip.

## 2. API routes

- [x] 2.1 Create `src/routes/orbitalConfigs.ts` (`createOrbitalConfigsRouter`). It has GET/POST `/configs` and PATCH/DELETE `/configs/:id`, with zod validation (a name of 1–40 characters after trimming, a record of number/boolean/string values, an 8 KB cap). Status codes: 201/200/204, 400 for invalid input, 404 for a missing id, 409 for a duplicate name, 403 for renaming or deleting the Default. Mount it in `src/app.ts` at `/api/games/orbital-dodger`, behind the existing `/api/games/*` auth. Verify: `orbitalConfigs.test.ts` in `src/routes/` covers every status code, including 401 without a session.
- [x] 2.2 Document the four routes and the `OrbitalConfig` schema in `openapi.yaml`. Verify: `npm test` passes, including any openapi coverage test (see `api-docs`).

## 3. Client config logic

- [x] 3.1 Add `client-games/src/games/orbital-dodger/configs.ts` with `resolveTuning(stored)` (layers stored values over `DEFAULT_TUNING`, type-checks each key, drops unknown keys, and checks `controlMode`/`edgeMode` against the allowed options), `tuningEquals(a, b)`, and try/catch-wrapped `loadSelectedId`/`saveSelectedId`/`clearSelectedId` for the localStorage key `orbital-dodger:config-id`. Also add `pickConfig(configs, storedId)`, which returns the chosen config and whether the stored id was stale. Verify: `configs.test.ts` covers a missing key, a wrong-type key, an invalid enum value, an unknown key, the stale-id fallback to the Default, and localStorage throwing.
- [x] 3.2 Add `listOdConfigs`, `createOdConfig`, `updateOdConfig` and `deleteOdConfig` to `client-games/src/api.ts`. Surface 409 and 403 as distinguishable errors, so the panel can show a "name taken" message. Verify: the client type-check passes.

## 4. Scene and host

- [x] 4.1 In `OrbitalDodgerScene.create()`, read the initial tuning from `game.registry` (key `initialTuning`) and copy it into `this.tuning` before the first layout is generated, so fuel, shields and planet count all come from it. Fall back to the shipped defaults if the key is absent. Add `applyTuning(t: Tuning)`, which does `Object.assign(this.tuning, t)` and calls `newLayout()` if `awaitingLaunch` is set. Verify: in the browser (6.2), a selected config with a different planet count shows that count in the very first layout, and choosing another config before the first press redraws the layout.
- [x] 4.2 In `OrbitalDodgerGame.tsx`, load the panel with an unconditional `lazy(() => import('./TuningPanel'))`, and keep the `__orbitalScene` handle dev-only. At mount, fetch configs with an 8-second timeout, and show a loading state instead of `PhaserGame` until the fetch settles. Resolve the selection with `pickConfig`, clear a stale id, and pass the resolved tuning into the game via `buildConfig`'s `callbacks.preBoot` (`registry.set('initialTuning', …)`). Then mount `PhaserGame`. On failure or timeout, mount with the shipped defaults and pass an "unavailable" state to the panel. Verify: `npm run build -w client-games` succeeds and the production bundle has the panel chunk. In the browser (6.2), throttle or block the configs request and confirm the loading state, then the fallback after 8 seconds.

## 5. Tuning panel

- [x] 5.1 Read `kb/phaser-mobile-input.md` and confirm the new panel controls and modals follow its DOM-overlay pattern. Verify: a note in the PR or commit confirming the patterns were checked.
- [x] 5.2 Remove the dev-only framing from `TuningPanel.tsx`. Add a config section above Modes: a dropdown (the Default first), and the Save, Save as new…, Rename…, Delete and Revert actions. Hide Rename and Delete for the Default, show Revert only when there are unsaved changes, and show an unsaved-changes marker. Make "Reset to defaults" restore the shipped values without saving. Verify: in the browser (6.2), each control appears, and is hidden, exactly as the spec says.
- [x] 5.3 Build one in-panel confirm/name modal, reusing the export modal's backdrop, and use it for: saving over the Default (text: "This will replace the default config for all players"), delete confirmation, discarding unsaved changes when switching configs, and name entry for Save as new and Rename. Show server 409 and 400 errors inline. Verify: in the browser (6.2), cancelling each prompt changes nothing on the server.
- [x] 5.4 Refetch the list when the panel opens and after every mutation. After deleting the selected config, or finding that it has disappeared, apply and select the Default. If there are unsaved changes, keep them in play as unsaved against the Default and show a notice (see design D6). Persist every selection change with `saveSelectedId`. Verify: in the browser (6.2), delete the selected config, then reload, and the Default is shown and applied.
- [x] 5.5 Move the panel's open state up to `OrbitalDodgerGame.tsx`, so `TuningPanel` takes `open`/`onOpenChange`. Wrap the HUD, the `PhaserGame` container and the leaderboard/end-of-run overlays in one absolutely positioned play-area `div`. Add a `ResizeObserver` on the outer container. While the panel is open, set the wrapper's `right` to `clamp(width − min(width, height × GAME_W / GAME_H), 0, PANEL_W)`, and set it to `0` while the panel is closed. Refit Phaser with a `ResizeObserver` on the play area that calls `scale.getParentBounds()` then `scale.refresh()` (see design D9). Put the pure inset function in `configs.ts`, or a sibling layout module, with unit tests. Verify: the inset tests cover wide (the full panel width), medium (partial, with the game flush left) and phone (0) sizes, and check that the displayed game width never changes. In the browser (6.2): at 1440×900 opening the panel moves the game left and none of it is covered; at a medium width it sits flush left with minimal overlap; closing recenters it; and at 390×844 the panel overlays as before.

## 6. Docs and verification

- [x] 6.1 Update `llm-context.md`: the tuning panel ships in production, configs are shared and server-stored with a protected Default, the selection is per browser, and there are new routes. Add a "leaderboard integrity under custom configs" item to `docs/games/planning.md`. Verify: both files read correctly.
- [x] 6.2 Stand up a second instance following `docs/dev-second-instance.md`. Use a fresh SQLite file, and not the developer's running server or DB. Drive the game with playwright-cli, saving screenshots to `/tmp/track-verify/`. Cover: the Default present on a fresh DB; create, rename, save and delete; the Default save prompt, both confirmed and cancelled; the selection persisting across reload; the stale selection falling back to the Default; the loading state before start, and the first layout using the selected config; the fallback when configs are blocked; reselecting before launch regenerating the layout; the side-by-side layout at a wide desktop size (1440×900), including a press on the moved game still steering correctly, a medium width with the game flush left; the overlay at phone size (390×844); and resizing the window while the panel is open; and the panel being present in a production build (`vite preview` or build output). Verify: screenshots, plus a list of what was checked.
- [x] 6.3 Run `npm test` and `npm run build`. Both must pass with zero TypeScript errors. Verify: the command output is clean.
