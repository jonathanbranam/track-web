## 1. Implementation

- [ ] 1.1 In `client-watch/src/pages/RatingsPage.tsx`, add `Link` import from `react-router-dom`
- [ ] 1.2 Add a "Browse →" link in the page header area pointing to `/movies/catalog`, matching the style from the old `MoviesWatchlistPage` (`text-sm text-violet-400 hover:text-violet-300`)

## 2. Verification

- [ ] 2.1 Build the watch client (`npm run build:watch`) and confirm zero TypeScript errors
- [ ] 2.2 Verify the Browse link appears at the top of the Ratings page and navigates to the Movie Catalog
